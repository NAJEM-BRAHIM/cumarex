<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- ================================================================
             SALAIRE NET (À PAYER)
             = SBG - CNSS - AMO - CIMR - Mutuelle - IR - Retenues
             ================================================================ -->

        <record id="ma_rule_salaire_net" model="hr.salary.rule">
            <field name="name">Salaire Net à Payer</field>
            <field name="code">NET</field>
            <field name="sequence">500</field>
            <field name="category_id" ref="hr_payroll.NET"/>
            <field name="condition_select">none</field>
            <field name="amount_select">code</field>
            <field name="amount_python_compute">
# Toutes les retenues sont déjà négatives, on les somme à SBG
def safe(var_name):
    return locals().get(var_name, 0) or 0

# Avantages en nature : à déduire du net car déjà ajoutés au brut mais pas réellement versés en cash
avn_logement = AVN_LOGEMENT if 'AVN_LOGEMENT' in locals() and AVN_LOGEMENT else 0
avn_voiture = AVN_VOITURE if 'AVN_VOITURE' in locals() and AVN_VOITURE else 0

retenues = 0
for code in ['CNSS_SAL', 'AMO_SAL', 'CIMR_SAL', 'MUTUELLE_SAL', 'IR_NET',
             'AVANCE', 'ACOMPTE', 'PRET', 'SAISIE', 'ABS_NP']:
    val = locals().get(code)
    if val:
        retenues += val  # déjà négatif

result = SBG + retenues - avn_logement - avn_voiture
            </field>
            <field name="struct_id" ref="hr_payroll_structure_ma_employee"/>
            <field name="appears_on_payslip">True</field>
        </record>

    </data>
</odoo>
